export interface PasswordsAndConfirmPassword {
    current_password: string;
    new_password: string;
    confirm_password: string;
}

export interface Passwords {
    current_password: string;
    new_password: string;
}